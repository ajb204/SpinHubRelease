# Domain model

Canonical destination for analysis mode, dataset topology, dimension semantics and pseudo-axis concepts. Root modules remain active compatibility locations while imports are migrated incrementally; this avoids combining file moves with semantic changes.
