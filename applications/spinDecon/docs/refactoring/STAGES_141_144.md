# Refactoring stages 141-144

## Stage 141 - Phasing service boundary
- Added GUI-independent `analysis/phasing_service.py`.
- Added `phasing` to `ApplicationContext` and the analysis-service composition root.
- Phasing no longer reads `parent.tabOne` or stores `self.tabOne`.
- Fixed a latent runtime defect: the migrated Phasing workspace referenced `topology_for`, `context_for`, and `project_for` without importing them.

## Stage 142 - Slice1D service-only scientific access
- Slice1D now always has a `SliceService`, including standalone/legacy construction.
- Removed stored `self.tabOne` and direct fallback reads for peaks, thresholds, labels, deconvolution state, axis limits, and reference views.

## Stage 143 - OneD service-only scientific access
- OneD now always has an `OneDService`.
- Removed direct fallback reads from the NMR workspace for peaks, thresholds, and axis limits.

## Stage 144 - Projection peak-authority correction
- Removed the stored Projection `self.tabOne` compatibility handle.
- Projection overlays now use the authoritative Full Peak List.
- Legacy `conn_data` is explicitly excluded from active Projection peak overlays.

## Validation
- Baseline Stage 140: 419 passed, 1 skipped.
- Stage 141: 422 passed, 1 skipped.
- Stage 142: 423 passed, 1 skipped.
- Stage 143: 424 passed, 1 skipped.
- Stage 144: 425 passed, 1 skipped.
- No regression accepted.
- Whole-tree byte compilation and AST parsing pass.

## Remaining active cleanup
The package/file architecture is complete. Remaining work is chiefly compatibility cleanup in Peak Review/Fit, Slice2D, Pseudo2D, and historical CPMG/Decay paths, followed by wx-enabled macOS golden-journey validation. MAGMA, Slice4D, uSTA, and conn_data-centric functionality remain deliberately quarantined as legacy.
