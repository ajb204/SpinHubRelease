# Stage 20 - repository hygiene completion

Moved the three historical noise-analysis notes to `docs/investigations/` and
the bundled c288 source archive to `archive/releases/`. Generated Python and
pytest caches are excluded from delivery. This completes repository hygiene
items that remained at the source root in the Stage 17 checkpoint.

No executable Python behaviour changed in this stage.
