# Stages 127-129

## Stage 127 - Peak workspace service boundary

Expanded `analysis/peak_service.py` with workspace-neutral operations for calibrated axis limits/scales, spectrum-file resolution, reference-peak-list opening, and reference peak publication. Corrected the service's parameter-store import to the canonical `project.parameter_store` location.

Peak review now routes decon peak-file resolution, status refresh, peak naming labels, 4D alias bounds, bore scales, and reference-list opening through `PeakService`. The source-contract regression for the Reference Peak List button was updated to protect the new service boundary rather than the retired direct `tabOne` call.

## Stage 128 - Notebook naming cleanup

The application notebook's AddTab/AddMagma compatibility method parameters are now named `nmr_workspace` instead of `tabOne`. They remain optional because the notebook's `_replace_tab` method owns construction from the canonical `self.nmr_workspace`.

The workflow controller now resolves `nmr_workspace` first and uses `tabOne` only as an explicit third-party compatibility fallback.

## Stage 129 - Peak review presentation cleanup

Remaining active Peak Review label and alias-bound accesses were moved behind `PeakService`. Compatibility fallbacks remain in construction and old standalone paths, but ordinary application execution no longer needs those direct workspace fields for these operations.

## Validation

- pytest: 404 passed, 1 skipped, 0 failed
- compileall: pass
- AST parse: pass

## Progress

The package/layout refactor is approximately 90-93% complete. The active architecture is now in its target package structure and protected against imports from `Frames`, root compatibility modules, and `misc`.

Remaining work is primarily dependency retirement rather than filesystem reorganisation:

1. Reduce the remaining `legacy_nmr_workspace` bridge in specialist workspaces where it represents live state rather than compatibility construction.
2. Audit uSTA/CPMG/Decay connectivity code. `conn_data` must remain legacy; useful connectivity behaviour should eventually be restored behind a dedicated connection/NOE service referencing Full Peak List identities.
3. Remove remaining executable Peak Review/Peak Fit fallback paths once standalone compatibility requirements are decided.
4. Final legacy/compatibility inventory and documentation.
5. Run real wx-enabled GUI golden journeys on representative 1D, 2D, 3D, pseudo-2D and pseudo-3D projects before declaring the refactor complete.
