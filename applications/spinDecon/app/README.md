# Application shell

This package will own application startup, the main frame/notebook, dependency
construction and `ApplicationContext`. `decon_tab.py` remains the active shell
until workflow routing and `tabOne` dependencies are migrated safely.
